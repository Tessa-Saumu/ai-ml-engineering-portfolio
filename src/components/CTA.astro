---
interface Props {
  title: string;
  body: string;
}

const { title, body } = Astro.props;
---

<section class="cta-block" aria-labelledby="cta-title">
  <h2 id="cta-title">{title}</h2>
  <p>{body}</p>
  <a class="text-link" href="mailto:theresia.saumu@gmail.com">Email me →</a>
</section>
