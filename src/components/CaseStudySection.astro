---
interface Props {
  number: string;
  title: string;
  text: string;
}

const { number, title, text } = Astro.props;
---

<section class="case-section">
  <p class="section-index">{number}</p>
  <div>
    <h2>{title}</h2>
    <p>{text}</p>
  </div>
</section>
